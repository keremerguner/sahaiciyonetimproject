import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        margin: 10,
        backgroundColor: '#e0e0e0',
        borderRadius: 5,
        flexDirection: 'row',
        borderWidth:1,
        borderColor:'gray'
    },
    input: {
        flex: 1
    }
})